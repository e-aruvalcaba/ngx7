import { CmxDataTableColumnDirective } from './cmx-data-table-column.directive';

describe('CmxDataTableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new CmxDataTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
