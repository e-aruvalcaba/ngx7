import { Observable } from 'rxjs';
import { ILanguageDirection } from '../interfaces/languageDirection.interface';
import { TranslationService } from './translation.service';
export declare class DirectionService implements ILanguageDirection {
    private translation;
    private direction$;
    private rtl$;
    constructor(translation: TranslationService);
    getDirection(): Observable<string>;
    getRTL(): Observable<boolean>;
    getDirectionFor(languageCode: string): void;
    getRtlFor(languageCode: string): void;
}
