import { PipeTransform } from '@angular/core';
import { FormatterService } from './../services/formatter.service';
import { TranslationService } from './../services/translation.service';
export declare class CmxCurrencyPipe implements PipeTransform {
    private formatter;
    private translation;
    private platformId;
    window: any;
    constructor(formatter: FormatterService, translation: TranslationService, platformId: any);
    transform(value: any, countryCode?: string): string;
}
