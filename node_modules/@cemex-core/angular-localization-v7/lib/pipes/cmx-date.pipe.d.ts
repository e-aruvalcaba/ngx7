import { PipeTransform } from '@angular/core';
import { DateFormatterService } from './../services/date-formatter.service';
import { TranslationService } from './../services/translation.service';
export declare class CmxDatePipe implements PipeTransform {
    private translation;
    private dateFormatter;
    private value;
    private monthsNames;
    private daysNames;
    constructor(translation: TranslationService, dateFormatter: DateFormatterService);
    transform($value: Date | string | number, $countryCode?: string): string;
}
