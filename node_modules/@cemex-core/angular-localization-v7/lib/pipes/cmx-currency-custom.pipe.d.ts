import { PipeTransform } from '@angular/core';
import { FormatterService } from './../services/formatter.service';
export declare class CmxCurrencyCustomPipe implements PipeTransform {
    private formatter;
    private value;
    constructor(formatter: FormatterService);
    transform($value: any, $currencySymbol: string, $decimalNumbers: string | number, $thousandSeparator: string, $decimalSeparator: string, $floatCurrencySymbolRight?: boolean): string;
}
