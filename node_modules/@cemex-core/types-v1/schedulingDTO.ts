import { PlantDTO } from './plantDTO';
import { DriversDTO} from './driverDTO'
export interface summaryDTO{
   drivers?:number;
   status?:string;
}
export interface scheduleStatus{
   scheduleStatusId:number,
   scheduleStatusDesc?:string
}
export interface SchedulingDetail{
   scheduleDetailId?:number,
   scheduleDetailTime?:string,
   scheduleStatus?: scheduleStatus,
   driver?:DriversDTO,
   scheduleNote:string,
   editing?:boolean,
   crudOperation:string
}

export interface SchedulingDTO {
   scheduleId: number,
   scheduleDateTime: string;
   plant?: PlantDTO;
   summary?: summaryDTO;
   scheduleDetails: SchedulingDetail[]
 
}
