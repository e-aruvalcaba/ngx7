export interface DocumentZipRequest{
    documents: Array<any>; // arreglo con los ducmentos
    documentTypeCode: Array<any>; // configDocuments, // arreglo con los tpos que vas a descargar
    countryCode: string;
}

export interface RequestFileDTO{
    guidRequestFile: string;
}

export interface ZipFileDTO{
    fileName: string;
    fileContent: string;
    filesToProcess: number;
    totalFiles: number;
}