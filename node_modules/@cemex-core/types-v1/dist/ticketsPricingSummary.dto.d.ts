export interface TicketUnit {
    unitCode: string;
    unitId: number;
}
export interface TicketSummary {
    deliveryQuantity: number;
    productDesc: string;
    totalPrice: number;
    unit: TicketUnit;
    unitPrice: number;
}
export interface TicketsPricingSummary {
    ticketsSummary: Array<TicketSummary>;
}
