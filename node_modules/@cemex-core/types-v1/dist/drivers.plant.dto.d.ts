import { PlantDTO } from './plantDTO';
import { UserDTO } from './userDTO';
import { IOrderTruck } from './drivers.order.dto';
export interface IPlantOptions {
    plantId: string;
    plantCode: string;
    plantDesc: string;
    monitoredBy: UserDTO[];
    status: boolean;
}
export declare class DriverPlantDto extends PlantDTO implements IOrderTruck {
    trucks: any[];
    constructor(option?: IPlantOptions);
    readonly plantDescView: string;
}
