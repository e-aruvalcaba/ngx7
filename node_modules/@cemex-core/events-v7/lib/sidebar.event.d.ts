import { Router } from '@angular/router';
import JobsiteDTO from '@cemex-core/types-v1/dist/jobsiteDTO';
import CustomerDTO from '@cemex-core/types-v1/dist/customerDTO';
export declare class SidebarEvents {
    private router;
    private jobsiteSubject;
    private customerSubject;
    private customersSubject;
    private jobsite$;
    customer$: import("rxjs").Observable<CustomerDTO>;
    constructor(router: Router);
    selectJobsite(jobsite: JobsiteDTO, redirect?: boolean, lock?: boolean): void;
    selectCustomer(customer: CustomerDTO, redirect?: boolean): void;
    setCustomers(customers: any): void;
    getJobsiteSelected(): import("rxjs").Observable<JobsiteDTO>;
    getCustomerSelected(): import("rxjs").Observable<CustomerDTO>;
    selectCustomerByCode(code: any, redirect?: boolean): void;
    currentCustomer(): CustomerDTO;
    currentJobsite(): JobsiteDTO;
}
