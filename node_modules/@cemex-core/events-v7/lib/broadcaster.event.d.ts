import { Observable, Subject } from 'rxjs';
export interface BroadcastEvent {
    key: any;
    data?: any;
}
export declare class Broadcaster {
    static CHANGE_SCREEN_HOME: string;
    static DCM_APP_LOGOUT: string;
    static DCM_LANGUAGE_CHANGE: string;
    static DCM_LANGUAGE_FETCHED: string;
    static DCM_LEGAL_ENTITY_CHANGE: string;
    static DCM_LANGUAGE_SELECTION: string;
    static _eventBus: Subject<BroadcastEvent>;
    constructor();
    broadcast(key: any, data?: any): void;
    on<T>(key: any): Observable<T>;
}
