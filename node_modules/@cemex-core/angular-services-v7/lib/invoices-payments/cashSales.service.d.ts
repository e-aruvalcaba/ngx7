import { EndpointsConstants } from '@cemex-core/constants-v2/dist';
import { CashDocument } from '@cemex-core/types-v2/dist/invoices-payments';
import { HttpCemex } from '../http.service';
export declare class CashSalesService {
    private http;
    private endpoints;
    private apiCashDocuments;
    constructor(http: HttpCemex, endpoints: EndpointsConstants);
    getCreditDocs(companyCode: string, customerCode: string, jobSiteCode: string): Promise<CashDocument[]>;
    private handleCashDocumentsData;
    private getUrl;
}
