import { Observable } from 'rxjs';
import { ICountryConfig } from '@cemex-core/types-v2';
import { HttpCemex } from './http.service';
import { ProjectSettings } from './project.settings';
import { SessionService } from './session.service';
export declare class CountryConfigService {
    private http;
    private projectEnv;
    private sessionService;
    private _countryConfig;
    private _countryConfigApiPath;
    constructor(http: HttpCemex, projectEnv: ProjectSettings, sessionService: SessionService);
    readonly countryConfig: Observable<ICountryConfig>;
    getCountryConfiguration(): Observable<ICountryConfig>;
    private handleError;
}
