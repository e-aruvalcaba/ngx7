import { Observable } from 'rxjs';
export interface ISearchHeaderResult {
    header: string;
    subHeader: string;
    data: any;
}
export interface ISearchHeaderService {
    executeService(searchValue: string): Observable<ISearchHeaderResult[]>;
}
