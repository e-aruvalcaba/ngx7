import { AlertType } from '../models/alert';
import { AlertRef } from './alert-ref';
import { BehaviorSubject } from 'rxjs';
export declare class AlertService {
    alerts$: BehaviorSubject<AlertRef[]>;
    alerts: AlertRef[];
    constructor();
    openSuccess(alertMessage: string, actionMessage: string, duration?: number, alertType?: AlertType): AlertRef;
    openError(alertMessage: string, actionMessage: string, duration?: number, alertType?: AlertType): AlertRef;
    openWarning(alertMessage: string, actionMessage: string, duration?: number, alertType?: AlertType): AlertRef;
    openInfo(alertMessage: string, actionMessage: string, duration?: number, alertType?: AlertType): AlertRef;
    private open;
    private _setTimeout;
    removeAlert(alert: AlertRef): void;
}
