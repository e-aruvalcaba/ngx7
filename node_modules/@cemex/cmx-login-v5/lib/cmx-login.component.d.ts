import { OnInit, EventEmitter } from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { FormGroup, FormBuilder } from '@angular/forms';
import { SessionService } from '@cemex-core/angular-services-v7';
import { TranslationService } from '@cemex-core/angular-localization-v7';
import { Broadcaster } from '@cemex-core/events-v7';
import { Language } from './model/language';
export declare class CmxLoginComponent implements OnInit {
    translationService: TranslationService;
    private fb;
    private sessionService;
    private route;
    private eventBroadcaster;
    private router;
    dirAttr: string;
    title: string;
    width: string;
    forgotPasswordUrl: string;
    appName: string;
    validateTemporaryPassword: boolean;
    urlRoute: string;
    rtl: boolean;
    loginForm: FormGroup;
    loginFailed: boolean;
    submitting: boolean;
    currentLang: Language;
    errorMsg: string;
    cmxLogoUrl: string;
    showPassword: boolean;
    loggedIn: EventEmitter<{}>;
    private generateCookie;
    private _rtl;
    private routeAfterLogin;
    private euHost;
    private ameHost;
    private euRegion;
    constructor(translationService: TranslationService, fb: FormBuilder, sessionService: SessionService, route: ActivatedRoute, eventBroadcaster: Broadcaster, router: Router);
    authenticate(): void;
    ngAfterViewInit(): void;
    /**
     * Go to forgot password, default to /forgotPassword unless a custom absolute url is defined
     */
    goToForgotPassword(): void;
    ngOnInit(): void;
    private getLogo;
}
