import { OnDestroy, AfterContentInit, QueryList, Renderer, ViewContainerRef } from "@angular/core";
import { CmxDropdownItemComponent } from "./../cmx-dropdown-item/";
export declare class CmxDropdownComponent implements AfterContentInit, OnDestroy {
    private renderer;
    dynamicComponentContainer: ViewContainerRef;
    _title: string;
    _placeholder: string;
    showItems: boolean;
    displayItems: string;
    title: string;
    rtl: boolean;
    closeOnClick: boolean;
    enableSearch: boolean;
    placeholder: string;
    maxLength: number;
    disabled: boolean;
    dropdownItems: QueryList<CmxDropdownItemComponent>;
    _enableSearch: boolean;
    viewportHeight: number;
    searchValue: string;
    private itemsPositioner;
    private _rtl;
    private _closeOnClick;
    private _maxLength;
    private _disabled;
    private originalPlaceholder;
    private changeSubscription;
    private childHeight;
    private _selectedItem;
    constructor(renderer: Renderer, isRTL: boolean);
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    close(event: any): void;
    open(event: any): void;
    filterDropdownItems(searchTerm: string): void;
    checkIfClose($event: Event): void;
    private setselectedItem(id);
    private processOptions();
    private truncate(value);
    /**
     * @description Checks whether to move the container of the items if it is outside the viewport
     */
    private isInsideViewport();
}
