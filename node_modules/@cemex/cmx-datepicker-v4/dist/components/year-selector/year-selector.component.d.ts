import { OnInit, EventEmitter } from "@angular/core";
import { DateNamesService } from "./../../services/date-names.service";
import { IYear } from "./../../models/datepicker-interfaces";
export declare class YearSelectorComponent implements OnInit {
    private dateNames;
    currentDate: Date;
    max: Date;
    min: Date;
    selectedYear: EventEmitter<number>;
    actionView: EventEmitter<string>;
    currentMonth: string;
    currentYear: number;
    yearsRange: IYear[][];
    rangeIncrement: number;
    private _max;
    private _min;
    constructor(dateNames: DateNamesService);
    ngOnInit(): void;
    previousYearsRange(): void;
    nextYearsRange(): void;
    monthToggle(): void;
    yearToggle(): void;
    selectYear(year: IYear): void;
    private reset;
    private getYearsRange;
    private modifyYearsRange;
}
